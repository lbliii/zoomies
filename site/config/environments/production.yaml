# Production Environment
# =======================

site:
  # GitHub Pages project site: deployed at /zoomies
  baseurl: "/zoomies"

build:
  incremental: true
  strict_mode: true

assets:
  minify: true
  fingerprint: true
